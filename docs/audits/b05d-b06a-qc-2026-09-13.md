# B05d / B06a QC — September 13, 2026

B05d fixes QC-13 and adopts typed request-auth clients (AUD-21), PR #108 / `e724161`. B06a fixes QC-02 calendar-day rendering, PR #110 / `863fab8`, issue #109. No SQL/data rollout. Final production state is in the release handover.

## Automated acceptance
- 279 isolated checks passed: prior 256 + 16 grape/auth + seven date cases. Real Supabase client transport tests cover bearer verification and token forwarding without cookies; shared resolver tests retain bearer precedence, cookie fallback and disabled modes. Grape cases cover canonical/alias results, deduplication/ranking, limits, empty/non-searchable queries, auth denials, generic query failures and both missing-reference-table 503s.
- Date contract runs under America/New_York, America/Los_Angeles, UTC, Asia/Tokyo, Pacific/Kiritimati and Pacific/Apia. Cases include July 8, year boundary, leap day, DST changes, Samoa's skipped calendar day, legacy ISO timestamps with opposite offsets and invalid dates. Instant controls confirm the process timezone changes take effect. This is not six browser/device timezone runs.
- Five schema/type-source checks (isolated replay/drift/source contract), database compile checks, whole web/mobile TypeScript/lint, Next production build and Expo web/iOS/Android Hermes exports passed. No schema changed or baseline was applied to production.
- GitHub CI passed for both product heads: runs `34741425769` and `34741673071`. Both primary `cellar-snap` previews passed. The duplicate `cellarsnap` preview failed in both; CLI build logs confirm missing Supabase environment variables on prerender, existing OPS-01. No hosting configuration changes or CI bypass.

## Actual authenticated HTTP
The local Next runtime used hosted Auth/Data API with designated account A. A separately authenticated SSR cookie jar and a cookie-free Authorization request returned identical results for Nebbiolo, Syrah, an actual alias-only key (`abernet auvignon` → Cabernet Sauvignon), unknown text and punctuation. Anonymous and invalid-token-only requests returned 401; invalid bearer plus valid cookie retained the existing 200 fallback. Query `ca` returned eight by default, two with limit 2, and eleven with limit 999 (under max 20). B05d production retest passed after merge. [Sanitized local results](../remediation/evidence/b05d-auth-parity.json); final live evidence is linked from the release handover.

The first harness expected the unverified synonym Spanna to exist. HTTP correctly returned matching empty results; the harness switched to an observed alias row. This was a fixture assumption, not an auth failure. A separate historical seed bug was confirmed and recorded as QC-14: uppercase letters are stripped before lowercasing. Correctly spelled Shiraz returned no suggestions; damaged existing keys are searchable. Tests do not claim the synonym corpus is correct.

## Hands-on web and Expo
- **Next desktop 1440×1000 / phone 390×844:** designated login, cellar-add Nebbiolo suggestion and selection, unknown input/clear interaction. No entry was submitted. Actual viewport/document bounds were checked; screenshot widths were verified after correcting which tab the viewport override targeted.
- **Expo production web with cookies removed from API requests:** login → Explore search Nebbiolo → authorized grape detail; cellar-add Nebbiolo autocomplete and selection. Phone Explore and desktop cellar-add screenshots inspected. Proxy logs confirm Authorization is present and incoming cookies are removed before forwarding. Native behavior is not inferred from shared localhost cookies.
- **B06a Next Library:** existing Proof Private entries reproducibly displayed Jul 7 before the repair. Searching Proof Private after the fix returned six fixtures dated Jul 8 at desktop and phone sizes. Clicking the first opens entry detail with Date consumed Jul 8. Date detail and Library screenshots inspected. The existing fixtures were neither edited nor deleted.
- **B06a rebuilt Expo:** Opened entry list → search Proof Private → six results, all Jul 8, at desktop and phone widths; select first → Date consumed Jul 8. Source-identical shared formatter is exercised in both runtime consumers. Screenshots inspected.
- No captured browser warnings/errors on the affected search/date tabs before cleanup; bounded server review showed successful affected routes and no application exception. After Expo sign-out, the still-open local web session received 401s and the existing palate loading fallback showed zero/unknown profile data. Explicit web sign-out cleared it; this cleanup observation is not a successful fresh auth check or a new date regression. Existing AUD-39/QC-03 session/count scope remains. Fresh production login is retested during release verification.
- Existing QC-03 count differences, QC-05 router labels, QC-07 absent mobile grape relation, QC-10 auth overflow, QC-12 image sizing and public numeric rating scope remain independently open. Successful date display does not certify those behaviors.

## Coverage and cleanup
`xcrun simctl` unavailable; no installed Android emulator/SDK found at the configured/common locations. No native device/emulator, OS login, gestures, binary/OTA distribution or native acceptance claim. Exports and Expo web are explicitly fallback coverage. Native receipt of the shared source change remains a release obligation.

No intentional entry/profile/reference-data mutations. Existing designated fixtures were read only; normal application reads may update their normal derived caches. API test sessions used local-scope sign-out; Expo and web UI sessions explicitly signed out. No env file edits; Expo API origin and cookie stripping existed only in temporary local test processes. Final service shutdown, original-file restoration, browser cleanup and production sign-out are recorded in the release handover.

Optional local screenshots/logs: `/tmp/cellarsnap-b05d` (web/Expo autocomplete, HTTP, build/CI evidence) and `/tmp/cellarsnap-b06a` (Library/detail/Expo dates, builds, tests). This checked-in report is sufficient to reproduce the findings without those temporary artifacts.
