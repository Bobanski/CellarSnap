# B05a tooling boundary QC — September 13, 2026 UTC

AUD-49, issue #101 / PR #102. Product `0487d5a`, based on B04d release `3527208`; merged as `960caaab773acb98d18648dbf9999702211b4a80` at 03:59:05 UTC. No SQL, dependency-version or product feature changes.

## Implementation and contract evidence
- Metro retains the workspace root for shared packages/root node_modules and preserves SDK blockList entries. An additional separator-bounded `.claude` regex excludes agent checkouts from the initial map, resolution and watch events. POSIX and Windows path shapes are tested, including similarly named non-checkout paths that must remain visible.
- Tailwind has an explicit source exclusion relative to `src/app/globals.css`, independent of Git ignore behavior. `.claude/worktrees/` is also ignored by Git; the existing checkout remains on disk. [Tailwind source rules](https://tailwindcss.com/docs/detecting-classes-in-source-files).
- Existing root ESLint/TypeScript exclusions, mobile project root/type source scope, and Playwright `e2e` testDir already bound their scans. Next 16.3.5 route watcher restricts itself to configured files/app directories; module watchers follow imported dependencies. No unsupported Turbopack ignore option or webpack override was introduced. Actual default Webpack Fast Refresh and Turbopack production build were checked below; future explicit imports from agent checkouts are outside the application contract.
- **3 new CI tests passed** using actual Metro and Tailwind packages, not just regex assertions. A disposable Metro file map omits nested sources, receives a real shared-file modification, and emits no nested-file create/change events. Tailwind's actual compiler/scanner omits a checkout-only utility while retaining the real-source utility.
- First test harness run used an outdated Metro event shape and omitted its cache directory; corrected to installed Metro 0.84.5 `changes.*Files` event contract and created the cache directory. A root require path was also corrected. Retest passed locally and on Linux GitHub CI.
- Actual repository file map: baseline **43,405 total / 660 `.claude` files**, candidate **42,745 total / 0 `.claude` files**. Shared index and private decoder remain visible in both. One local crawl was 604 versus 534 ms; these timings are not a performance conclusion. The asserted improvement is exclusion of independent checkout files.

## Checks and browser testing
- **266 tests**: 256 existing isolated, 7 B04d dependency contracts, 3 tooling contracts. Web/mobile lint and types, Next production build, and fresh Expo web/iOS/Android Hermes exports passed. GitHub run `34736652248` both jobs passed on `0487d5a`; working primary preview passed. Duplicate hosting still fails under OPS-01.
- All 149 non-Hermes files in the 151-file Expo export are byte-identical to B04d, including web JS/assets. The two Hermes outputs were regenerated successfully; no byte-identity or native execution claim.
- Hands-on Next development browser: at localhost:3002/login, a temporary visible marker in real login source appeared through Fast Refresh while a disposable nested checkout contained invalid TSX. Restoring the original file also refreshed back. Phone 390×844 and desktop 1440×1000 layouts visually inspected; recovery-link navigation worked, phone body/document width 390. Fixtures removed and original login source verified unchanged.
- Initial dev use of 127.0.0.1 was denied access to dev assets by Next's default origin protection, so no HMR occurred there. Retested on permitted `localhost`; no configuration/security exceptions added. Later stopping that dev server stranded its temporary browser tab on an error page; final production testing used a fresh tab. This is harness lifecycle, not an application regression.
- Fresh optimized Next at 3001: designated A login, phone feed and loaded photos (40 decoded images observed), desktop feed layout, menu/sign-out passed. Both viewport/document widths matched (390 and 1440). Screenshots visually inspected; no captured browser warnings/errors or local production server errors.
- Fresh Expo export at 8083: designated A login/feed, Cellar tab and existing 71-entry list, menu/sign-out passed. Phone/desktop authentication forms inspected. Existing authentication decorative overflow remains QC-10: width 390/document 450 and width 1440/document 1500. B04d QC-12 blank single-photo feed layout remains separate; B05a did not alter the web JS. No new captured Expo warning/error; the reused tab retains the earlier B04d generic Event entry, which is not evidence of a new B05a error.
- Available native runtimes rechecked in B04d: no usable iOS simulator, Xcode or Android SDK/emulator. Exports and tooling tests are fallback coverage, not native UI acceptance or distribution. AUD-49 is a development-tooling finding; native distribution remains on AUD-08.

## Cleanup and release
No entry/scan/account data mutations; designated sessions signed out. No environment-file changes. Temporary HMR source and disposable `.claude/worktrees/b05a-disposable-qc` removed. `next dev` appended generated guidance to AGENTS.md; after stopping dev, restored the exact original tracked file. Preexisting user tsconfig edit and personal plans/reports remain untouched; existing nested checkout remains, now intentionally Git-ignored. Optional evidence `/tmp/cellarsnap-b05a/`.

Merge is complete. Production deployment and live acceptance are recorded in the [combined release handover](../remediation/handovers/b04d-b05a-release.md); no migration required. Rollback is a revert/redeploy of tooling/CSS-source configuration, with no data reversal; it would restore unwanted checkout scanning.
