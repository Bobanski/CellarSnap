# B06e earned-feature authority QC — September 13, 2026

Implementation `63cb2c6`, resumed checkpoint `3df3298`, PR #117 / #112. Additional real PostgreSQL concurrency checks live in `scripts/schema/replay-postgres.mjs`.

- 385 isolated checks, nine schema tests, whole web/mobile lint and types, database type contracts, Next production build, Expo web/iOS/Android exports pass.
- PostgreSQL 17.6 baseline/forward replay matches catalog. Four independent-backend races cover feature-first and revocation-first deletion/key reassignment, explicitly observing the waiting backend lock. Revocation-first rejects the subsequent feature; feature-first commits then revocation removes it.
- Live forward SQL applied as `20260913074139`; statement SHA-256 `319f843212eee7da071905ec27657f7bc0cf301ae1bf6f165bba378f4edcf71c`, matching the file. Hosted structural drift checker matches. Security advisor names/counts/details unchanged (timestamps ignored); existing [public projection advisory](https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view) and other backlog advisories remain.
- 29 hosted Data API/HTTP checks pass: ordered/single/clear synchronization, malformed/unearned/cross-owner denial, cookie/bearer legacy/modern API parity, unauthorized denial, ten concurrent feature/deletion or reassignment races. Retry of a possible deadlock preserves authority. Original five award IDs/timestamps and both profile fields restored exactly; only named disposable race awards were inserted/deleted.
- Hands-on Next production-build browser: 1440×1000 desktop feature three awards/save; reload, deselect/reselect to reorder; 390×844 phone persisted order, cancel retains saved list, clear/save/reload returns empty. Dark layout remains intact. Screenshots in `/tmp/cellarsnap-b06e-resume/desktop-featured.png` and `phone-featured.png`; no captured console errors/warnings or production-server errors.
- QC-15: clean dev server 3004 profile load and repeated reloads also return 200 without JSON parsing errors. Prior single transient 500 did not reproduce. No speculative product fix; unknown original cause retained in history.
- Current Expo production web: designated login, collection (5/85 earned), First Pour detail and bearer/cookie-stripping logs pass. There is no reachable feature/clear control: the handler is unused. QC-16 tracks this pre-existing limitation separately. Legacy mutation contract is HTTP-tested, not falsely claimed as a UI pass. No Xcode/simctl or Android emulator runtime available; exports and responsive/Expo web are not native acceptance or distribution.
- Harness corrections: sign-in returns session tokens without embedded user/cookie jar; validated user via Auth and used SSR setSession to generate real cookies. Corrected test-only unknown badge ID `five-hundred-logs` to defined unearned `five-hundred`. Final rerun passes; no authority source change was required.
- GitHub web/mobile CI and primary `cellar-snap` preview pass. Duplicate `cellarsnap` fails missing Supabase env vars, matching OPS-01; not bypassed or reconfigured. Merge and final production verification are recorded separately in the release handover.

Follow-up intake: a working production-build summary fault proxy reproduces QC-03's remaining null-as-zero palate routing error. B06f owns that fix; B05e aliases remain separate.
