# B06b / B06c / B06d QC — September 13, 2026

## Scope and tested versions

Three bounded slices under [issue #112](https://github.com/Bobanski/CellarSnap/issues/112): AUD-09 badge evaluation/award authority, QC-03 own-account counts, and QC-08 selected event navigation. Reviewed heads: B06b `640338eef47fc7cc8095235c756fdf42be8f9339` (#113), B06c `626ee6057414f52587ac1a486887094257d06cec` (#114), B06d `a4f20ebca2db88a0c0febb03338a5be006192f94` (#115). Final product merge `b51ae782bfd941afeb8c954d58927b9f58965263` has identical affected product/schema/test source to the final reviewed head. Source checkpoint handovers retain slice-specific details.

## Automated and build checks

- **385 isolated checks passed**: prior 279 plus 96 badge, six summary and four event-selection contracts. Badge checks classify every one of the 85 definitions, cover all 12 trigger families, exact normalized category matches, compound/distinct-entry/min-region/min-producer constraints, pagination under short server caps, error propagation, and inserted-only concurrent awards. PGlite exercises the actual forward migration against anon/authenticated insert/upsert/update/delete/truncate and preserved service-role behavior.
- Summary checks cover 1,007 entries with an artificial 37-row service cap, canonical country preference/raw fallback, normalized distinct countries, bidirectional accepted-friend deduplication, pending requests, owner award filters, empty results and query failures. Event checks cover A/B, stable-ID reorder, restricted slides, context and zero-photo fallback.
- Five schema/source tests, database compile contract, whole web/mobile TypeScript and lint, Next production build, and Expo web/iOS/Android Hermes exports passed. Final profile-error isolation and mobile photo-control wrapping were included in repeat type/lint/build/export checks; final Expo bundle was reloaded and context/public navigation retested.
- GitHub CI runs `34742827821`, `34743129428`, `34743628811` passed web/mobile jobs. Primary `cellar-snap` previews succeeded. The duplicate `cellarsnap` target failed again; inspected build output reports missing Supabase environment variables on prerender (existing OPS-01). No bypass or hosting-project reconfiguration.

## Hands-on local browser and Expo web

Designated accounts and exact disposable fixtures were used. No existing entry or relationship was edited to make results agree.

| Surface | Exercise and observed result |
|---|---|
| Next desktop 1440×1000 and phone 390×844 | Badge library shows 8 of 85 with Nebbiolo Head, Campania Curious and Chile Committed; Taste/Region filters and earned badge detail work. Menu, profile and palate agree on complete owner totals; profile gallery pagination does not determine totals. |
| Next event gallery, desktop/phone | Search `B06d QC` in My Events. Select public/private wine and follow Open details: matching entry heading, private share disabled. Context image has Event context caption and an instruction to select a wine, with no unrelated detail link. |
| Rebuilt Expo web, desktop and phone | Cookie-stripping proxy proves bearer requests for badge/summary APIs. Menu/profile/Library agree at 88 entries, three countries and two friends with all fixtures present; badges show eight earned and Nebbiolo detail opens. |
| Expo event gallery, phone 390×844 | Reorder persisted slides to private/context/public. Labeled photo controls select each matching image/caption; public and private actions reach their own details. Final rebuilt bundle repeats context/public navigation successfully. |
| Error inspection | No captured warnings/errors on affected browser tabs; local server log search found no error/warning/500/503 entries during these checks. |

Screenshots were visually inspected. An immediate desktop slide screenshot captured the image before it loaded; retaken after loading, Wine A and the public caption/link align. The first positive badge fixture setup used invalid grape position zero; corrected to one and rerun without duplicating entries. An initial test expectation used a display-derived badge ID; corrected to the unchanged canonical `first-log` ID. These QC setup/test errors did not require production data repair.

A local summary-outage reverse proxy returned 503 but did not hydrate the Next client. It provides **no browser error-state acceptance**; query-failure contracts pass, and that live visual fallback still needs a working fault-injection harness. Existing Expo `(app)` header (QC-05), settings glyph and route-stack Back behavior were not redesigned; broader navigation/presentation scope remains separate. Broader page-based average/top-grape/top-region calculations remain AUD-35, and session bootstrap remains AUD-39. Sparse synthetic details show unavailable palate-match data; scoring acceptance is outside these slices. Production sign-out invalidated the earlier Expo API session: its summary rendered em dashes and requests denied 401. A fresh Expo login restored 200 summary/detail requests and the expected counts; final navigation was retested with that valid session. This is an auth-expiry observation, not acceptance of the separate browser 503 harness.

## Hosted acceptance

Primary production deployment **`dpl_2mmNXsfTW4ztLghCRgmVDoUuCiJ7`** is Ready at `https://cellar-snap-ivfglhy88-eitan-sneiders-projects.vercel.app`, aliased to cellarsnap.app. GitHub deployment `6418650956` binds it to final product merge `b51ae78`, success at **06:53:56 UTC**.

- Fresh production login, phone badge filter/detail and menu counts, desktop profile totals, desktop public selected-event detail, phone context/private detail, and explicit sign-out to login passed. Images/captions and detail headings were inspected; no captured affected-tab warning/errors.
- Cookie and cookie-free bearer summary requests agree exactly: 88 entries, three countries, two friends, zero incoming pending requests, eight badges. Querying an arbitrary other user ID does not change the own-account result; anonymous and invalid bearer requests return 401; responses use no-store.
- Owner gets three authorized event slides; designated other account gets two and no private sibling. Reordered selections passed against the final deployment.
- After the SQL rollout, authenticated Data API insert/upsert/update/delete on user_badges and anonymous reads return 42501. Concurrent server evaluations, including forged owner/count/rating hints, return exactly the three legitimately qualifying QC awards once in total. Historical award rows/timestamps remain identical. This suite passed again against the final production product deployment.
- A bounded error-level Vercel query for the exact final deployment since 06:53:56 UTC returned zero records (limit 100). This is a bounded observation, not a claim about all production traffic.

## SQL, fixtures and remaining coverage

Forward-only migration `20260913061822_server_authoritative_badge_awards.sql` was applied once after #113 merged; hosted migration version **20260913064845**, name `server_authoritative_badge_awards`. Local SQL and recorded remote statement SHA-256 both equal `535ab04b29381ed7af478d0a54610e6be4cd9a3bc3257a0251ee271089444846`. Client grants are authenticated SELECT only, existing authenticated read policy retained, client insert policy removed, service-role access retained. Historical baseline SQL was neither edited nor replayed.

Security-advisor names, counts and finding metadata were unchanged before/after. Existing findings include the public_profiles security-definer view, mutable function search paths, public extension, function execute grants and disabled leaked-password protection. The rate-limit table's no-policy notice is existing backend-only behavior. No whole-project security-clean claim. See [database linter guidance](https://supabase.com/docs/guides/database/database-linter) and [password protection guidance](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).

Fixture cleanup is recorded in the release handover: 15 badge tasting entries and only three newly awarded badges; two event entries, one group, three slide/photo objects and their metadata. Existing five awards/timestamps are preserved. Normal app reads may update derived caches.

No working iOS simulator (`xcrun simctl`) or Android emulator is installed. Expo web and Hermes exports do **not** establish native gestures, OS auth, binary/OTA distribution or native acceptance. AUD-09 remains Partial: 53 definitions supported, 32 explicitly deferred; historical award correctness and direct featured-profile authority remain separate work. QC-03/QC-08 remain Partial for native runtime/distribution; the summary-failure browser experiment is also explicitly unverified. [Release handover](../remediation/handovers/b06b-b06d-release.md).
