# B03a personal knowledge read QC — September 12, 2026

Product head `b4feb47`, base `70c0721`, PR #90 / issue #89. Scope AUD-04 reads, targeted AUD-19/48. Lifecycle remains B03b.

- 201 automated tests passed; five new tests run the actual migration and pgvector search against isolated PostgreSQL. The vulnerable baseline positively disclosed a synthetic owner's chunk to another user and to service general search. Fixed matrix covers both owners, public/private parent, forged JSON owner, orphan/unknown sources, anonymous, writes/TRUNCATE, service/general/personal search, threshold, limit and migration replay.
- Web lint, TypeScript and production build passed. GitHub web and mobile type/lint jobs and primary `cellar-snap` preview passed. Duplicate Vercel `cellarsnap` still fails (OPS-01); preserve separate project pending its investigation.
- Exact SQL deployed as remote `20260913002605`, MD5 `2c4ab9fbdf82841d6a768ba19012613d`, matching reviewed local file. No personal/curated rows rewritten by B03a. Captured preflight catalog has 172 personal + 3,349 curated chunks, zero orphans. No private contents exported.
- 13 live Supabase Data API/RPC checks passed on disposable A/B entries and curated fixture: own-only reads, cross-target denial, backend general-search exclusion, anonymous denial and denied client update. Public parent does not make personal embeddings public. Test account privileges were not changed.
- Hands-on local Next browser at desktop 1440×1000: login, sommelier owner request and streamed response passed (synthetic `B03 Amber Lantern` returned `apricot lantern`). Phone 390×844: clear chat, general acidity advice and second-account privacy request passed; B received only its own `blueberry compass`, never A's marker. Composer, send/loading state, nav, clear and sign-out were exercised. Screenshots visually inspected; dark Noir theme preserved. Browser warning/error logs empty; bounded server error search empty.
- Fresh Expo web export against local API: login and owner chat passed at desktop and phone size. Resizing required a reload to settle browser emulation; retested after reload. Existing `(app)` heading (QC-05) and Animated/useNativeDriver web-fallback warning remain. No native iOS/Android tooling installed (`simctl`, `adb`, `emulator` unavailable); this is web fallback, not native or OTA acceptance.
- Security advisors: seven categories, 42 observations after versus 44 before; two mutable-search-path function warnings removed, no new category. Remaining findings retain their canonical IDs. [Advisor reference](https://supabase.com/docs/guides/database/database-linter).

Evidence screenshots/logs: `/tmp/cellarsnap-b03-qc/` (optional local artifacts). Repeat using designated E2E accounts and synthetic B03 entries; live reads do not depend on screenshots. Fixtures retained only for the immediate B03b lifecycle tests and must be removed at session completion. Browser model answers complement deterministic access assertions; they are not the proof of isolation.
