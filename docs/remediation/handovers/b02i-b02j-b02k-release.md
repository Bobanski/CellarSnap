# B02i / B02j / B02k release — September 13, 2026

## Objective and IDs
Three bounded slices completed: adopted photo metadata without Storage signing (AUD-01), canonical photo-reference inventory before rekey (AUD-01/22), and web-feed numeric rating containment (QC-01). Related AUD-19/21/48/50. AUD-49 is reopened for a confirmed historical Git-index residual; no unrelated cleanup was implemented. Features and Noir Refined theme retained. This release supersedes release-pending statements in [B02i](batch-02i.md), [B02j](batch-02j.md), and [B02k](batch-02k.md).

## Resume here
Product main **`c7dcfd23e8c4e00d5903ce9462153047919add94`**, tree identical to tested **`18814594826ca3501214bb92b0a5c086da2bfd1d`**. #138/#139/#140 are merged/closed; both application CI jobs and primary previews passed. The primary site is Ready and live-verified on cellarsnap.app. [Sanitized combined evidence](../evidence/b02i-b02j-b02k-release.json). One additive RPC migration is live and full-catalog verified. No native runtime acceptance, binary/OTA distribution, production rekey or installed-client cutoff. Preserve design draft #75 and open residual issues #81/#104/#112. This session's one-time merge/close permission expires at handoff.

## State and decisions
- **B02i:** previous adopted payloads minted then discarded Storage signatures. `readable_wine_photo_paths(text[])` now checks existing Storage SELECT/RLS and source authorization under SECURITY INVOKER, empty search path, authenticated-only EXECUTE, maximum 100 bounded paths. Server adapters deduplicate/chunk requests, retain missing/denied nulls, and never fall back to signing on RPC failure. Independent request-time byte authorization remains. Legacy bearer clients/other buckets retain compatibility. This removes the adopted server's unnecessary signing, not every direct/native legacy minting path.
- **B02j:** a read-only operator SQL/CLI inventories all nine canonical physical path fields, shared references, every wine-photo object and original siblings. Explicit libpq target, repeatable-read/read-only transaction, query/process deadlines, 10,000-row and 64MiB bounds. Private mode-0600 snapshots are fsynced and published atomically without overwriting existing files; stdout only contains aggregates/digest. Restart inventory after interruption; it is not a durable rekey ledger. No mutation/deletion command exists.
- **Inventory:** 894 keys / 882 objects / 650 canonical references / 277 originals / 140 multiply referenced keys. Twelve referenced keys lack an object; 118 unreferenced non-original objects need review. Cover paths are covered but currently empty. Eight likely URL/opaque columns returned zero raw-existing-object or `/wine-photos/` candidates. This does not establish external/encoded-consumer completeness or make unreferenced objects safe to delete. Missing references are recorded under existing AUD-01/AUD-22 without attributing new data loss to this session. [Inventory evidence](../evidence/b02j-inventory.json).
- **B02k:** cookie-web `/api/feed` returns `rating: null` plus a server-derived `public_rating_label`. The current web client preserves all four enjoyment bands, QPR, notes, groups and pagination. Owner 1–100 editing elsewhere is unchanged. A previously loaded old frontend can omit the band until refreshed; no raw-number compatibility opt-out exists. Direct Data API reads, Expo's direct feed queries, other API/detail/profile payloads and physical rating privileges remain open. **QC-01 remains Partial; this is endpoint containment.**
- **AUD-49 P2/Partial:** final CI passes but checkout post-job cleanup warns exit 128 because historical tracked `.claude/worktrees` gitlinks have no `.gitmodules` URL. Three gitlinks and three local settings files remain tracked despite ignores. Prior B05a Metro/Tailwind/watch acceptance stays valid. Reopened the same root ID with evidence and a separate B05 follow-up; preserved actual local worktrees/files.

## Verification
- Final application tree: **412 isolated checks, 21 schema tests**, web types/lint/production build, database compile/type-source contracts, mobile types and both GitHub application CI jobs pass. Actual **PostgreSQL 17.6** full catalog replay, owner/stranger checks, four prior badge races, two prior atomic-edit races, and real CLI Unicode/private/exclusive snapshot publication pass. New SQL's hosted catalog exactly matches the reviewed additive delta; generated hosted type output differs only by the new RPC signature. Advisor categories/counts are unchanged (six categories, seventeen observations); no new advisor finding from the RPC.
- **Hosted RPC:** E2E owner/public/private-photo/private-entry/test-author/block/restore, anonymous/oversized denial and deleted-object null checks pass. Unit adapter fixtures prove batches 100/100/5, partial failure isolation, duplicate/unrequested-path handling and zero Storage signing requests on the adopted path. Existing source relationship/shared-copy/original matrices pass unchanged. Four alternating single-path timing pairs are observations, not a speedup benchmark.
- **Actual local and live photo QC:** desktop 1440×1000 / phone 390×844 cookie-web and the unchanged B02g Expo browser export render real wine photos, preserve original dimensions during crop/save/reopen, and deny/restore the same URLs after committed privacy/test-author changes. Expo outage/retry, denial/retry and actual sign-out pass; no browser Storage requests and adopted API payloads contain no capabilities. Old bearer payload compatibility passes. Live photo checks used primary B02j `a2b50aa`; B02k changes no photo code. [Live web](../evidence/b02i-live-web-phone.png), [live Expo crop](../evidence/b02i-live-expo-crop-phone.png).
- **Actual local and final-primary feed QC:** every numeric rating field in serialized feed payloads is null; all four labels display at desktop/phone widths, real wine images render, grouped A/B selection changes heading/counter and restores, All/My Circle switches, cursor pages do not overlap and Load more appends. Own/private/test-author/blocked fixtures are excluded; legitimate friend entries remain. Owner phone rating 92→93 persists and reloads correctly. [Live phone](../evidence/b02k-live-feed-phone.png), [live desktop](../evidence/b02k-live-feed-desktop.png). Initial live captures awaited an avatar/unfinished photo fade; the harness was corrected to await the exact wine image's decoding and opacity 1, then the complete live feed run passed again. No application fix was required. Group behavior has explicit interaction assertions; screenshots scrolled past the group are not claimed as full-card visual proof.
- **Errors:** zero page exceptions in photo/feed acceptance. Live Expo photo logs contain the four deliberate 404s and injected 503. Cookie photo and local owner edit retain the earlier TypeError Failed to fetch diagnostic; cancellation traces do not prove its cause (AUD-50). The final repeated live feed run has zero page/console/HTTP failures. Local Next logs have no application errors. Final primary's bounded error query, 23:16:57–23:20:46 UTC, limit 100, returned zero records; that does not erase browser diagnostics.
- **Coverage limits:** Xcode apps/simctl and Android emulator/AVD runtimes are absent. Responsive browser and an existing Expo web artifact are compatibility coverage, not native acceptance or distribution. This release does not certify unrelated scoring, lifecycle, detail/profile payloads or galleries. The photo fixture's not-ready score is expected sparse-fixture behavior. Public numeric Data API access remains a known QC-01 residual.

## Release state
| Slice | PR | Tested head | Merge / UTC |
|---|---|---|---|
| B02i | [#138](https://github.com/Bobanski/CellarSnap/pull/138) | `3686ac9` | `552d541` / 23:03:41 |
| B02j | [#139](https://github.com/Bobanski/CellarSnap/pull/139) | `2d3b067` | `a2b50aa` / 23:09:45 |
| B02k | [#140](https://github.com/Bobanski/CellarSnap/pull/140) | `1881459` | `c7dcfd2` / 23:16:31 |

Exact SQL `20260913225201_request_photo_paths.sql` is hosted as **`20260913225511`**, statement MD5 **`4d4abaee0bee864881e8c05d1f790d32`**; full local SHA256 is in combined evidence. SQL preceded application adoption. No source-row rewrite, Storage policy/grant expansion, object retirement, client cutoff or signature rotation. B02j/B02k have no SQL. Do not replay historical migrations to reconcile timestamps. Roll forward; retain the invoker/current-access boundary and safe numeric projection.

Primary production **`dpl_GBvwhP2CyLffim2uk2qC6tV7LQFc`**, GitHub deployment **`6427650763`**, Ready for `c7dcfd2`, succeeded **23:16:57 UTC**; aliases include cellarsnap.app. Photo acceptance used unchanged source on prior primary **`dpl_4Qix5iFbWx4q5mETJSgaWqgY3bPv`** (`a2b50aa`), Ready 23:10:08 UTC. B02i primary succeeded 23:04:14 UTC. OPS-01 duplicate `cellarsnap` previews still fail for missing Supabase environment variables; all three failure logs were inspected. Primary `cellar-snap` succeeds. No hosting/routing settings changed.

## Workspace and environment
Original modified tsconfig.json is byte-identical to session-start backup; both user reports, nested worktree and design draft #75 remain. No env files changed; only command-scoped local proxy settings. Temporary Next 3001 and Expo/proxy 8083 stopped; unrelated services preserved. Named session entry/group counts are zero, Storage objects removed through its API, temporary tester flags restored. Local harnesses/logs `/tmp/cellarsnap-b02i-b02j`; only sanitized evidence is committed. Raw operator inventory files must remain private and uncommitted. Detailed CLI snapshot publication was exercised on local PG; hosted inventory returned aggregates only.

## Next slice
1. **AUD-01 P0:** review inventory/external consumers and the twelve missing references; implement durable copy/hash/conditional reference swap and pending-revocation operation states using the [existing contract](../b02h-photo-revocation-contract.md). Recheck references under source locks; stable mapping must include originals and every canonical/derived reference. Establish supported native distribution/adoption and legacy signing cutoff before production retirement. Require races/lost-response retries and multi-region revocation observations; no retained-byte recall claim.
2. **QC-01:** coordinated owner-only physical rating protection and compatible projections for mobile, remaining API/detail/profile consumers. Inventory all reads/writes before revoking base-table/column SELECT, which affects owner writes and installed SDK clients. Feed-only nulling is not closure.
3. **AUD-13/15:** atomic grouped edits and remaining cellar/lifecycle/import commands with explicit side-effect ownership; keep native acceptance separate.
4. **AUD-49:** a separate bounded B05 Git-index cleanup preserving local worktrees and existing watcher contracts. B07 scoring, deferred badge facts and broader B05 bootstrap/adoption remain in the [canonical backlog](../backlog.md).
