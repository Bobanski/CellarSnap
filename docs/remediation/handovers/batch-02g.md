# B02g handover — September 13, 2026
## Objective and IDs
AUD-01 P0/Partial: adopted bearer/mobile request-time photo delivery. Related AUD-48/50. Preserve features and Noir Refined. Legacy capability revocation, installed native adoption/distribution and numeric rating privacy remain separate.
## Resume here
`codex/b02g-bearer-photo-delivery`, base main `e2efe31`, issue #81. Implementation and local production/Expo QC passed; merge/deployment/live verification pending. One-session user permission to merge and close PRs is active during this session only. Next B02h documents measured legacy revocation and its staged rollout prerequisites; B08c takes ordinary web details/grapes, with group/lifecycle work deferred.
## State and decisions
- `X-CellarSnap-Photo-Delivery: request-v1` explicitly opts verified bearer clients into signature-free API photo payloads. Unadopted binaries retain their existing payload contract. Cookie-web continues request delivery.
- Mobile wine-photo path adapters issue app image URLs without Storage signing. Shared Image wrapper converts configured legacy Storage URLs (including transformed URLs) to app paths, discarding signatures. Only exact configured HTTPS app origin (or loopback development) receives bearer headers; no query tokens, cookies, redirects or unauthenticated remote fallback. External public reference images and local camera/upload previews retain existing handling.
- Browser/native fetch uses bounded in-memory data URLs, no shared/disk cache, 15-second deadline and visible retry. Mounted images are keyed by URI/session token/attempt; stale session results cannot render. Unmount aborts pending fetch; sign-out clears the image surface; foreground activation rechecks. Mounted/retained bytes are not a global revocation guarantee. Native fetch lacks streaming; server and native blob checks cap encoded output at 12 MiB. Existing server input bounds remain 24 MiB and 40M decoded pixels.
- Crop preview, size lookup and byte reads use the original-dimension variant; newly adopted display resizing does not downsample the crop source. Local source-data reuse preserves the previous recrop behavior. No new native dependency or persistent image cache.
- Image endpoint permits cross-origin bearer GET/preflight without credentialed CORS. Explicit invalid bearer cannot fall back to an ambient cookie. Old app response capabilities, direct SDK permissions and retained/cached URLs remain AUD-01 residuals.
## Verification
409 isolated checks, web production build, web/mobile types/lint and Expo web/iOS/Android exports pass. [Sanitized actual browser evidence](../evidence/b02g-qc.json). Desktop 1440×1000 and phone 390×844 Expo: label renders, zero browser Storage requests, bearer-only image requests. Browser cross-origin fetch to the real app endpoint passes. Exact same URL denies after committed photo privacy, entry privacy and test-author changes, restores afterward; denial/retry and injected 503/retry pass. Adopted bearer detail payload has no Storage signatures; unadopted payload remains compatible. Actual menu sign-out clears images. Owner phone crop saves/reopens, using original-dimension requests. No page exceptions; four deliberate 404s and one injected 503 account for viewer console failures.
Cookie-web desktop/phone regression passed image privacy/restore, signature-free payloads, zero Storage requests, crop save and original reopen. One browser Supabase fetch error accompanied navigation/closure cancellations; no page exception or server error. This is not represented as a zero-console-error run. Native simctl/Xcode and Android SDK/emulator unavailable: Expo exports/browser QC are not native acceptance/distribution. Broader native crop/gallery and per-screen runtime acceptance remain pending.
QC caught low-contrast retry text, corrected to the existing light text token and visually retested. Review also preserved original-dimension crop reads before the final Expo/crop retest. Existing Expo router labels and desktop cover cropping remain unchanged QC scope; unrelated feed zero-height images remain QC-12.
## Release state
No SQL or dependency changes. PR/merge/deployment/live verification pending. Server negotiation is safe before new-client distribution. Roll forward if a surface fails; do not restore signatures to adopted clients or bypass current authorization.
## Workspace and environment
Original user `tsconfig.json`, untracked `cellarsnap-fix-plan.md` / `cellarsnap-qa-report.md`, nested worktree and design draft #75 preserved. No env files changed. Command-scoped Expo API override only. Temporary Next 3001 / Expo 8083 and harnesses under `/tmp/cellarsnap-b02g-b02h-b08c`. Disposable entries/photos deleted; original test-account flags restored. Secrets/identities are not in committed evidence.
## Next slice
B02h legacy revocation evidence/contract, then B08c ordinary web atomic detail/grape adoption. QC-01 private numeric projections requires a coordinated physical privilege/projection and supported-client migration, remains P1/Partial rather than being silently folded into these slices. Group edits and other cellar/lifecycle commands stay AUD-13/14/15. Native distribution remains explicit across all repaired mobile findings.
