# B02m/B02n/B05f release — September 13, 2026 (EDT)

## Objective and IDs
QC-01 P1/Partial: contain numeric ratings in detail/profile/tagged responses and adopt the projected feed on mobile. AUD-49: finish the Git-index worktree boundary while preserving prior watcher acceptance. Related AUD-48/50. No SQL, physical rating isolation, production photo retirement or native distribution.

## Resume here
**#144/#145/#146 merged**, final product main `81a98e3ca0a158592428a7c6227f3992d3ce7cce`. Tree `d0c3197a19bf6a4af3d9acb13d864d8ab96be338` exactly equals tested candidate `d484287`; application implementation is `4c7a8de`. B02m merged as `99bb80a`, B02n as `51dafdc`, B05f as `81a98e3`. Both application CI jobs pass for each PR; B05f run `34793747682` also has clean checkout and post-job submodule cleanup. Primary final production is Ready; complete web and Expo-browser suites pass against it. [Sanitized release evidence](../evidence/b02m-b02n-b05f-release.json). This supersedes the three release-pending checkpoints. Preserve #81/#104/#112 and design draft #75. The user's one-session merge/close permission expires at this handoff.

## State and decisions
- B02m projects `rating` using authenticated viewer versus each entry's `user_id`. Owners keep the stored 1–100 value; other viewers receive `rating: null` and the existing enjoyment band. Profile ownership/tagging/friendship is not ownership of a tagged entry. Web and mobile profile consumers read `public_rating_label`. Installed older profile clients omit public bands until updated; do not restore private numbers for cosmetic compatibility.
- B02n adds bearer auth to `/api/feed`; an explicitly supplied invalid Authorization header cannot fall back to a cookie. All returned responses are private/no-store with read-only CORS; cross-origin mobile calls omit cookies. Mobile uses the existing API base/token/photo-adoption contract and opaque `cursor_v2`. It rejects wrong-viewer, raw-rating and invalid-band responses, surfaces bounded fetch/body timeout/errors, and has no direct SDK feed fallback. A stale load-more result cannot append after a scope/session change. Remaining detail interaction helpers are retained.
- Server group metadata supplies each visible slide's own notes, QPR and public band; slide `rating` is always null, including on owner detail group metadata. Main owner detail rating remains numeric. Context/missing-entry slides have null rating/band. Entry RLS/visibility and request-authorized photos remain the authority.
- B05f removes three historical Git links and three local settings files **from the index only**. Existing ignore/Metro/Tailwind/TypeScript boundaries are unchanged. All affected local paths/file hashes and the registered nested checkout are preserved.

## Verification
429 isolated tests, 30 schema tests and 3 existing tooling tests pass. Web production build/types/lint, database type contract, mobile types/lint and all-platform Expo exports pass. No migration or catalog change was made; this session does not claim a new live full-catalog audit. Supabase changelog and column-privilege guidance were reviewed before choosing compatible endpoint containment rather than silently revoking base-table SELECT.

Desktop 1440×1000 and phone 390×844 web, repeated on final cellarsnap.app: all four feed bands, null serialized ratings, images, group selection/captions, opaque cursor/no-overlap/Load more, All/My Circle, own/private exclusion, friend inclusion, test-author and block denial/restore, invalid bearer with valid cookie rejection and CORS preflight pass. Detail/profile/tagged cookie and bearer owner/non-owner matrices, mixed-owner tags, anonymous/private denial, public profile bands/navigation and owner 92→93 save/reload pass. Blocked authors' entries are denied; a viewer can still see their own row tagged with that author, consistent with owner RLS. No claim that an incoming block hides an owner's own data.

Desktop/phone Expo-browser feed: all four labels, group click and keyboard slide selection, per-slide notes/QPR/counter, visible decoded grouped images, real pagination/circle switching, zero direct `wine_entries` feed reads, bearer-only signature-free projected payloads, cross-origin requests, deliberate outage/recovery, delayed pagination after scope switch, comment post/read, test-author/block exclusion/restore, profile band counterpart and sign-out pass. Native Xcode/simctl and Android SDK/emulators are absent; browser tests and iOS/Android exports do not certify installed native runtime or distribution.

Known limits: QC-05 `(app)` header and QC-12 loaded ordinary feed images at zero height (nine measured at each viewport) remain B11; grouped images render. Local web feed owner run had the recurring handled `TypeError: Failed to fetch` diagnostic (AUD-50); final live web feed/profile runs have zero page/console/HTTP failures. Final Expo recorded one injected feed 503 and seven photo 404s, all seven explicitly during test-author denial; restore/recovery passed. No page exceptions. The final production error-log query (00:51:35–00:56:35 UTC, limit 100) returned zero records; do not infer a universally error-free app.

QC corrections: wrong fixture field and Playwright request API were repaired; age-gate hydration and virtualized-list scrolling required explicit waits/scrolling; phone profile screenshots now wait for image opacity, not decode alone. Full affected suites reran. Root build initially followed a type-only test import into native aliases; isolated transport types fixed that boundary. Request-ref cleanup lint warning was fixed with a stable invalidation callback, followed by lint/types/export/browser retests. No unrelated application fix was folded into these slices.

Fresh clone of prior `8f39e21` reproduces checkout cleanup exit 128 and `No url found for submodule path`; candidate has an empty worktree-metadata index and both cleanup/submodule status exit 0. Actual B05f web/mobile CI setup and post-job cleanup contain no matching warning. Prior Metro/shared-source/Tailwind behavior remains accepted. AUD-49 can close for this now-verified scope without deleting its reopened history.

## Release state
No database/schema/SQL migration or ordinary-object rewrite/deletion. Primary deployment `dpl_2miXAyN4tbETLzwdtP2EvdApyjrL`, GitHub deployment `6428448955`, for `81a98e3`, Ready September 14 **00:51:35 UTC**, aliases include cellarsnap.app. Final native-source Expo export is served locally through a cookie-stripping proxy to this exact production API; no native binary/OTA was published. All three primary previews pass. Duplicate legacy `cellarsnap` previews and final production fail with the confirmed missing Supabase variables (OPS-01); primary `cellar-snap` succeeds. Roll forward preserving null/public-band transport; never roll back to raw ratings as a cosmetic recovery.

## Workspace and environment
Original modified tsconfig bytes, two untracked user reports, registered nested worktree and #75 preserved. No environment files changed. Private harnesses/logs under `/tmp/cellarsnap-b02m-b02n`; committed evidence is sanitized. Disposable B02m/B02n rows/groups/comments and Storage objects removed, tester flags restored; independent final counts in evidence. Session Next 3001 and Expo 8083 stopped at final handoff; unrelated services preserved. Temporary fresh clone remains under the session's /tmp directory for reproducibility and contains no copied environment files.

## Next slice
1. AUD-01 P0: supported-native/client adoption, legacy signing cutoff, persistent late-write fencing, avatar/external-consumer mapping, retirement-worker acceptance and multi-region denial observations. B02l pending_revocation operations still do not authorize deletion; no production cutoff performed here.
2. QC-01 remains Partial: inventory every rating reader/writer, then owner-only physical rating storage/privileges with coordinated compatible clients. Direct Data API and mobile detail/library/remaining API projections are still open. Native consumers need a configured production `EXPO_PUBLIC_WEB_API_BASE_URL` and distribution; older installed direct-feed clients remain exposed.
3. AUD-13/15 grouped/lifecycle atomic commands. Keep AUD-10 scoring residuals and AUD-19/21 broader schema/type adoption on their existing IDs. B11 includes QC-12 ordinary-image sizing, QC-05 headers and QC-18 web input associations; don't infer their closure from these runs.

[Canonical backlog](../backlog.md) and [hub](../README.md) are updated. [Web profile phone](../evidence/b02m-live-profile-phone.png), [Expo group phone](../evidence/b02n-live-expo-group-phone.png), [Expo profile phone](../evidence/b02m-live-expo-profile-phone.png), [known QC-12](../evidence/b02n-qc12-expo-ordinary-phone.png). No new findings were allocated; known issues were updated under their existing IDs.
