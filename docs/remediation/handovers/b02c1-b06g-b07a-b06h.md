# B02c1 / B06g / B07a / B06h checkpoint — September 13, 2026

## Objective and IDs
Four separate bounded slices, based on main `3dc1234`: QC-01 mobile public rating presentation (#121/#81), QC-16 reachable Expo featured controls (#122/#112), AUD-10 grape resolver schema (#123/#104), and QC-08 grouped-feed scroll/controls found during QC (#124/#112). No SQL. Preserve all awards, owner 1–100 inputs, features, dark theme and design draft #75.

## Resume here
All four implementation PRs are stacked in that order; release is pending at this checkpoint. Source commits: `2fafda2`, `e8e9cc0`, `7fd0634`, `4c0383f`. See the [canonical backlog](../backlog.md). The session owner explicitly permits merging/closing PRs only for this session; that permission does not carry into a future session.

## State and decisions
- B02c1 uses the existing shared enjoyment bands for ordinary/grouped mobile feed labels. This is presentation only: raw feed/Data API payload rating exposure remains QC-01, and public identity projection remains AUD-06.
- B06g appends/removes earned badges from the ordered modern selection, preserves other picks, caps five, exposes clear, serializes writes and shows last confirmed state on failure. Read failures offer retry; controls do not appear while collection state is unknown. Server responses determine persisted selection. Existing authority and all historical awards remain unchanged.
- B07a removes nonexistent `grape_aliases.alias_type` from the actual typed join. Exact versus synonym provenance follows canonical spelling; raw ILIKE metacharacters are escaped and normalized keys use equality. Region/producer lookup semantics retained. Absent `ai_notes_summary`, mobile automatic OCR selection and broad scoring parity stay AUD-10/B07.
- B06h is a separately recorded QC repair. Browser wheel scrolling moved a 348px photo track to offset 350 but left selection at 1; selection depended only on native momentum. An overlapping decorative dot row also intercepted clicks. Normal scroll now selects the nearest slide; one named control row has 44×44 targets. Native momentum/tap guards retained. Event-list B06d history is not overwritten.
- Privacy re-triage: read-only hosted public_profiles still returns raw first/last names and test flag without viewer filtering. AUD-06 stays Open. Current Smart CDN docs explicitly warn cached signed responses may outlive token expiry until CDN cache expiry; AUD-01 stays P0/Partial. No policy relaxation, key rotation, destructive object invalidation or claimed global TTL cap. Follow [Storage signing](https://supabase.com/docs/guides/storage/serving/downloads) and [Smart CDN](https://supabase.com/docs/guides/storage/cdn/smart-cdn), reviewed September 13.

## Verification
- Combined `7fd0634`: 388 isolated checks, 12 schema tests, database query compile contracts, web/mobile types/lint, production Next build, all-platform Expo exports. The new resolver fixture's missing required fields were corrected during type verification; its 42-test suite was rerun successfully. B06h `4c0383f`: mobile types/lint and fresh web/iOS/Android exports pass; final CI recorded at release.
- Actual Chromium UI, desktop 1440×1000 and phone 390×844: web and Expo sign-in; web/Expo feed qualitative labels; Expo All/My Circle switch; grouped wheel/dot/Enter selection with counter updates. Screenshots visually inspected. This is web runtime QC, not native device acceptance.
- Expo badges: feature, append, reload, retain two picks on injected 503, retry, remove one preserving the other, five selected, clear/reload, detail/back, load outage/retry. Delayed write disables other controls and sends one request. Proxy strips cookies; badge calls carry bearer tokens. Original profile selection and complete badge response restored and compared. Four expected injected-503 browser errors; no page exceptions on this flow.
- Hosted actual resolver module: old query returns 42703; five canonical/provenance cases and four unmatched cases pass, including wildcard input. Desktop/phone browser edit/save of disposable private Syrah fixture returns 200, preserves 92 and grape, logs exact rather than stub, and reloads. Fixture, grape links and resolution logs removed; exact award IDs/timestamps unchanged. Harness corrections for 1-based grape positions, title-case normalization and DOM selectors are not application failures.
- Existing Maps loader/legacy Places warnings remain QC-04. WebSocket close warnings occurred during route/context transitions; no page exception on the final editor run. Native tooling remains absent (`xcrun simctl` fails; no Xcode/Android SDK/emulator). No native binary or OTA acceptance/distribution.

## Release state
Implementation and scoped local/hosted QC complete; PR merge, final primary deployment and live verification are pending at this checkpoint. No migrations or production schema changes. Primary previews are Ready; duplicate Vercel project fails with Missing Supabase environment variables, independently reconfirmed in current build logs (OPS-01). Do not change routing or retire that project without its separate contract.

Rollback is source-only; keep previously deployed database authority. Reverting B07a restores stub fallback; reverting B02c1 restores numeric public labels; avoid those regressions. Mobile source merge/export is distinct from distribution.

## Workspace and environment
Original user tsconfig.json preserved byte-for-byte; untracked cellarsnap-fix-plan.md / cellarsnap-qa-report.md and nested worktree preserved. Session Next production server 3001 and cookie-stripping Expo server 8083 are temporary. No environment files changed. Local evidence/scripts/screenshots are under /tmp/cellarsnap-next-batches; auth states and original fixture snapshots there are sensitive and must not be committed. Sanitized evidence and release outcome will be linked in the release handover.

## Next slice
1. Finish ordered PR release and live resolver/editor verification, then publish the release handover/backlog state.
2. Prioritize AUD-01 cache/delivery revocation and AUD-06/QC-01 coordinated public projections; presentation does not close these.
3. QC-07 is the next bounded data-loss-risk mobile slice: canonical grape reads and retention across unrelated edits. Keep scoring ai_notes_summary/parity, badge historical/deferred definitions and native distribution separately tracked.
