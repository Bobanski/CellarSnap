# B02l handover — September 13, 2026

## Objective and IDs
AUD-01 P0/Partial and AUD-22 P2/Open: read-only inventory triage plus durable, bounded entry-photo copy and atomic canonical reference swaps. Related AUD-19/48/50. No historical repair, ordinary-object rekey/deletion, native distribution or legacy cutoff. Preserve all features and Noir Refined. [Operator contract/runbook](../b02l-photo-rekey-operator.md).

## Resume here
Branch `codex/b02l-resumable-photo-rekey`, base `a8a24a1`, implementation `a512de1`, [PR #142](https://github.com/Bobanski/CellarSnap/pull/142), issue #81. Implementation/QC complete; merge/primary deployment pending at this checkpoint. One additive private migration is already live and catalog-verified. Final branch adds the missing-original/reference-bound regression and this evidence; no application-source difference from main. Keep #81/#104/#112 and design draft #75 open. Current user authorized this round's QC and merge when ready, excluding production-wide rekey and client cutoff.

## State and decisions
- Private, owner-only SECURITY INVOKER operator functions and a deny-all RLS ledger. No public RPC, application grants, scheduler or automatic work. Stable two-key mapping and fingerprints are stored before copy. Notes, ratings and identity fields are not copied into the ledger.
- Copy without overwrite, bounded streaming hash/size/type verification, repeat discovery after lost responses. One transaction locks the operation, canonical writer tables and existing object metadata, rechecks source/destination snapshots, and swaps every captured reference without replacing row IDs. Three real races reject stale entry edits, inserted references and Storage metadata changes.
- Stops at **pending_revocation**. No deletion command exists. Future/in-flight legacy writes are not fenced, avatars require a separate fixed-name authority contract, and external consumers/native adoption remain unproven. Do not treat a pending operation as deletion authorization or global revocation acceptance.
- Inventory review: twelve missing keys = ten slides (nine originals retained) + two ordered photos (no originals). All identify existing source entries; seven slides have a current same-type ordered photo, one is entry-less context. All 118 unreferenced non-original objects exceed 30 days; 117 entry-shaped paths lack source entries, one is avatar-shaped. 59 known prefix owners, no tester prefixes or originals. These are investigation leads, not repair mappings/deletion candidates.

## Verification
[Sanitized QC](../evidence/b02l-qc.json). 412 isolated checks; **30 schema tests**, including nine new workflow tests; web types/lint/build, database type contracts and mobile types/lint pass. Actual PostgreSQL 17.6 full catalog replay, three new rekey races plus four existing badge/two atomic-edit races, real CLI plan/retry/status/abandon and retirement refusal pass. Remote CLI requires verified TLS. Hosted all-nine-column/original copy with lost-copy and lost-commit responses, stable row IDs, owner/viewer privacy/test-author/block recovery and public-RPC exclusion pass. Catalog exactly matches reviewed additive delta. Advisor counts 17→18 in the same six categories: new INFO is intentional private-ledger RLS without application policies.

Actual Chromium desktop 1440×1000 and phone 390×844: local and live web rekeyed photo rendering, signature-free payloads, privacy/test-author deny/restore, anonymous denial, owner crop/save/reopen pass. Unchanged B02g Expo-browser export: desktop/phone real images, bearer-only requests, denial/outage retry, old-client compatibility, sign-out, original crop/save/reopen pass. [Live web phone](../evidence/b02l-live-web-phone.png), [desktop](../evidence/b02l-live-web-desktop.png), [Expo phone](../evidence/b02l-expo-phone.png), [reopened crop](../evidence/b02l-expo-crop-phone.png).

Zero page exceptions. Web crop issues one handled Storage-copy HTTP 400 for an existing original; application source explicitly handles this and save/reopen succeeds. Navigation/closure cancellations remain recorded. Expo's only HTTP failures are four deliberate 404s and one injected 503. A screenshot-only retest initially timed out because it required WebP while the reopened crop uses a JPEG data-URL prefix; corrected format-neutral image loading plus animation wait, inspected the full dialog, and reran the complete suite successfully. No application fix. Existing `(app)` header (QC-05), sparse-fixture not-ready scores and broader detail-rating privacy remain outside this operator slice. Server log shows no application errors.

Xcode/simctl and Android SDK/emulator are absent. Responsive web and Expo-browser compatibility do not certify native runtime acceptance or distribution. No deployment of new native source occurred.

## Release state
SQL `20260913234035_photo_rekey_operations.sql` hosted as **`20260913235129`**, statement MD5 **`52d2df95120d5fce99cdf449fbaf4bc3`**; exact SHA-256 in evidence. No existing-row mutation, policy/grant expansion or scheduled execution on install. Hosted fixture reference swaps occurred only on disposable E2E data and were cleaned through scoped row deletes and the Storage API. Full pre/post hosted catalogs verified. Primary application remained unchanged during live acceptance; merge/deployment state must be updated at release. Roll forward; retain ledger/mapping for any real pending operation, never restore retired keys.

## Workspace and environment
Preserve original modified `tsconfig.json`, both untracked user reports, existing nested worktree and design draft #75. No environment files changed. Local Next 3001 and Expo proxy 8083 remain during release checks; stop only these session processes before final handoff. Private harnesses/logs under `/tmp/cellarsnap-b02l`; committed evidence has no credentials/raw inventory. Hosted fixture rows/groups/collections, objects and fixture-only operation records removed; original profile values restored. Profile-flag fingerprint checked against the previous preserved baseline (`1732f025b055f58143ed175770ddbdff`, four testers).

## Next slice
1. Complete this release's merge/CI/primary verification and publish final status.
2. AUD-01: establish native client adoption and legacy-signing cutoff; design persistent late-write fencing, avatar mapping, external-consumer review and a resumable retirement worker with new-object acceptance and multi-region denial observations. No production retirement under this checkpoint.
3. QC-01: physical owner-only ratings and remaining mobile/API/detail/profile projections; then AUD-13/15 grouped/lifecycle commands. AUD-49 remains its own B05 Git-index cleanup, with existing watcher acceptance retained.
