# B02o/B02p/B02q release — September 13, 2026 (EDT)

## Objective and IDs
QC-01 P1/Partial: inventory rating consumers, contain Home circle JSON and adopt required owner-aware mobile detail reads. Related AUD-19/48/50. No physical rating migration, production photo retirement or native distribution. [Consumer inventory and cutover gates](../b02o-rating-consumers.md), [sanitized evidence](../evidence/b02o-b02p-b02q-release.json).

## Resume here
**#148/#149/#150 merged.** Product main `c8717ce44aaedeb0d3a6470df78859f38850141e`, tree `66d9cbe2b00a7c12f5634675b94d11096832c0f6`, exactly equals tested candidate `1561bbf`. B02o merged as `435d488`, B02p as `070ad0e`, B02q as `c8717ce`. Both web/mobile CI jobs and primary previews pass for all three PRs. Primary production is Ready and the complete affected Home/detail/web-profile journeys pass against it. This supersedes the three checkpoints. Preserve #81/#104/#112 and design draft #75. One-session merge/close permission expires at handoff.

## State and decisions
- B02o traces explicit rating readers/writers plus wildcard and row-snapshot dependencies across clients, APIs, knowledge/scoring and operator tools. Hosted table-level SELECT still permits authenticated direct rating reads on visible rows. Endpoint projection is partial containment; adding a private table while retaining readable legacy numbers would not fix this. No grants were changed.
- B02p Home keeps owner recent ratings and projects every circle rating to null plus its public band. Owner IDs and authenticated viewer ID accompany the payload. Mobile requires the projected API, validates ownership/bands/group slides, removes its direct-SDK fallback, clears failed lists and offers retry. Counts, QPR, photos, reactions and navigation are retained. Current Home is a mobile screen; there is no web Home page consumer.
- B02q mobile detail now starts with the projected detail API, validates requested entry/viewer/owner ratings and group slides, and displays the public band for other owners. It no longer reads a raw entry before optional API hydration. Required separate grape reads still fail closed; authorized photo/profile/reaction helpers and owner mutation commands remain. Owner 1–100 edit snapshots and crop behavior are retained.
- Both GET endpoints explicitly reject an invalid Authorization header without falling back to a valid cookie, return private/no-store and read-only CORS, and support signature-free adopted photo payloads. Mobile requests omit cookies. Shared read transport bounds token/fetch/body completion even if a dependency ignores abort; session/route/unmount invalidation discards late results. New mobile source requires the updated backend; no raw compatibility fallback.

## Verification
**466 isolated / 30 schema checks**, including 37 new transport cases; web/mobile types/lint, canonical database type contract and all-platform Expo exports pass. Exact-commit primary Vercel production builds and all application CI pass; a separate local Next production build was not run. Targeted hosted grants/function-source inventory was read-only; this session does not claim a fresh full-catalog audit. No SQL change.

Actual desktop 1440×1000 and phone 390×844 Expo-browser Home: all four circle bands, retained owner rating, decoded target images, reaction write/reload, detail navigation, zero direct Home entry reads, cross-origin bearer/no-cookie/signature-free payloads, private exclusion, test-author/block denial and restore, malformed/raw response rejection, injected outage/retry, sign-out and held-response rejection. Cookie/anonymous/invalid-bearer/preflight API counterparts pass. The final live Home run has zero page exceptions and two deliberately injected Home 503s only.

Actual desktop/phone Expo-browser detail: band instead of numeric rating/edit controls for non-owner, photos/grapes, projected groups, friends/private/test/block matrices, zero direct detail entry reads, comment persistence, outage/retry, raw/wrong-viewer rejection and delayed response after sign-out. Owner 92→93 save/reload and photo crop save/reopen pass. Final live detail has zero page exceptions; its four HTTP failures are the intended private/test/block 404s and one injected 503. Web desktop/phone detail/profile/tagged cookie and bearer owner/non-owner matrices, mixed-owner tags, navigation, owner edit/reload and explicit invalid-bearer rejection pass again on final primary, with **zero page/console/HTTP failures**.

Extended normal/bulk mobile editor regression uses the hosted atomic command: notes-only grape/link preservation, explicit replacement/clear, pre-save failure, lost-success retry, timeout, concurrent stale conflict, retained local input, stranger denial and web counterpart all pass. Normal runs record the deliberately injected 503s and expected PT409 conflicts; bulk retry finishes/publishes and preserves grape IDs. This verifies existing lifecycle behavior after the new read; AUD-13/15 remain Partial for grouped/broader lifecycle work and native distribution.

Native unavailable: CommandLineTools only; `xcrun simctl list devices available` cannot find simctl; emulator/adb and the standard Android SDK directory are absent. Expo browser plus iOS/Android exports are not installed-native acceptance. Known QC-05 `(app)` header and QC-12 ordinary feed sizing remain B11. No new finding ID was allocated.

QC harness corrections: fixed status-property use, waited for committed comment/save state instead of immediate UI feedback, supplied the separate editable photo record required by crop, and waited for the target Home image before screenshots. A pending interception raised a disposal error during the first live Home shutdown and interrupted cleanup. Six identified disposable entries, their group/photo were recovered; late interception disposal is handled, fixture manifests are persisted privately, and the **complete live suite reran successfully**. No application defect was patched after the tested product commit.

## Release state
Primary `dpl_9v61QSXbcM4Gkq52J28fA5NgHPKG`, GitHub deployment `6428892510`, is for `c8717ce`; Ready September 14 **01:39:18 UTC**, aliases include cellarsnap.app. The Expo export was served locally through a cookie-stripping proxy to that exact primary backend. No native binary/OTA was published. Duplicate legacy `cellarsnap` preview and production builds reproduce OPS-01 missing Supabase variables; primary `cellar-snap` succeeds. No hosting configuration changed.

No SQL/schema migration or ordinary data/photo rewrite/deletion. Roll forward while preserving null/public-band transport; do not restore public numbers to recover old-client labels. Runtime-log query coverage and counts are in the sanitized evidence; a bounded query does not establish universally error-free operation.

## Workspace and environment
Independent final counts: **zero session entries/groups/comments, 882 wine-photo objects, four testers**. Harnesses restored original designated flags and atomic fixtures/derived rows; badge awards unchanged in the atomic checks. Original modified tsconfig bytes, two untracked user reports, registered nested worktree and #75 are preserved. No environment files changed. Generated Next dev AGENTS delta was restored after shutdown. Next 3001 and Expo 8083 stopped; unrelated services preserved. Private harnesses/logs under `/tmp/cellarsnap-b02o-b02q`; committed evidence is sanitized.

## Next slice
1. AUD-01 P0: establish supported-native/client adoption and legacy signing cutoff, durable late-write fencing, avatar/external-consumer coverage and retirement-worker acceptance. B02l pending_revocation still does not authorize deletion or a global revocation claim.
2. QC-01: use the B02o inventory to move remaining direct owner library/create/edit/import consumers and privileged scoring/knowledge/operator dependencies to an owner-only rating contract. Coordinate atomic source transfer, snapshots/invalidation, minimum supported clients and direct REST/RPC/realtime role tests before physical cutoff. Public Home/detail source adoption is now complete; direct Data API and installed old clients remain exposed.
3. AUD-13/15 grouped/lifecycle atomic commands. Preserve AUD-10 scoring, AUD-19/21 schema adoption and B11 QC-05/12/18 residuals on their existing IDs.

[Canonical backlog](../backlog.md), [hub](../README.md), [Home phone](../evidence/b02p-live-home-phone.png), [detail band phone](../evidence/b02q-live-detail-band-phone.png), [owner crop](../evidence/b02q-live-crop-phone.png), [web profile phone](../evidence/b02q-live-web-profile-phone.png).
