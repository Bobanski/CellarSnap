/**
 * Standalone collections route — renders the entries screen
 * with the collections tab pre-selected. This is a hidden tab
 * so it doesn't highlight the Cellar tab in the bottom nav.
 */
export { default } from "./entries/index";
