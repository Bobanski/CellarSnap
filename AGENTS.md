# Repository Agent Rules

## Git Safety
- Never run `git commit` unless the user gives explicit approval in the current turn.
- Never run `git push` unless the user gives explicit approval in the current turn.
- If there is any ambiguity, ask for approval before either command.
